manifestVersion: subsquid.io/v0.1
name: aave-v2-liquidations-file-store
version: 1
description: |-
  A squid that indexes liquidation events on AAVE V2 and saves the resulting data locally
build:

deploy:
  processor:
    cmd: [ "node", "lib/processor" ]
